
						MINISHELL

- env: path | try void env (default path ?)

- signals
	- approfondir: SIGSEV / childs etc

- PS1 var + mini conf file | --opts
	- PS1 with basics (%~, %m …) formatter from zsh
	- alias
	- XXXX=xxxx vars (export)

					---------------

- INIT:
	- get env to struct env (?)
	- if no env, set default values
	- set signals handler

- LOOP:
	- set & print prompt
	- get & slit line
		- split for ';' or "&&"
		- thats commands; then split for ' '
	- for each cmd, find & exec
